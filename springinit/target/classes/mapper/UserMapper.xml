<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springinit.mapper.UserMapper">
    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" parameterType="string" resultType="org.example.springinit.pojo.User">
        select id, username, password, phone, role, address,status, create_time as createTime
        from t_user
        where phone = #{phone}
    </select>

    <!-- 新增用户 -->
    <insert id="insert" parameterType="org.example.springinit.pojo.User" useGeneratedKeys="true" keyProperty="id">
        insert into t_user (username, password, phone, role, address, create_time)
        values (#{username}, #{password}, #{phone}, #{role}, #{address}, now())
    </insert>
    <!-- 按账号（手机号/用户名）查询用户 -->
    <select id="selectByAccount" parameterType="string" resultType="org.example.springinit.pojo.User">
        SELECT username, password, phone, role, address, create_time,status
        FROM t_user
        WHERE phone = #{account} OR username = #{account}
    </select>
</mapper>